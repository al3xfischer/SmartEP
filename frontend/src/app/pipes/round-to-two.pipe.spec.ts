import { RoundToTwoPipe } from './round-to-two.pipe';

describe('RoundToTwoPipe', () => {
  it('create an instance', () => {
    const pipe = new RoundToTwoPipe();
    expect(pipe).toBeTruthy();
  });
});
